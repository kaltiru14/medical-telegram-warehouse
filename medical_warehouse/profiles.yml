medical_warehouse:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: postgres
      password: postgres123
      port: 5432
      dbname: medical_telegram
      schema: staging  # staging schema for dbt
